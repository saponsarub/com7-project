# ─────────────────────────────────────────────────────────────────────
#  แกนหลัก — พอสำหรับ ต่อฐานข้อมูล · จัดหมวดสินค้า ITEC ด้วยกฎ · ทำ Power BI
#  ML อยู่ในไฟล์แยก requirements-ml.txt เพราะหนัก ~2.5 GB
#
#  ล็อกเวอร์ชันขั้นต่ำเท่าที่ทดสอบแล้วจริงบนเครื่อง Sapon.S เมื่อ 2026-09-17
#  python 3.14.6
# ─────────────────────────────────────────────────────────────────────

pandas>=3.0.3
numpy>=2.4.6
pyarrow>=23.0.1          # pandas ต้องใช้ตอน to_parquet
PyYAML>=6.0.3            # rules.yaml ของ ITEC

# ── ต่อ SQL Server ───────────────────────────────────────────────────
# ⚠️ ต้องลง ODBC Driver 18 for SQL Server แยกต่างหากด้วย ไม่ใช่ pip
pyodbc>=5.3.0

# ── ไฟล์ Excel ───────────────────────────────────────────────────────
openpyxl>=3.1.5

# ── วาดภาพพื้นหลัง Power BI (Powerbi/_design/) ────────────────────────
pillow>=12.3.0

# ── โน้ตบุ๊ก ──────────────────────────────────────────────────────────
jupyterlab>=4.5.9
ipykernel>=7.3.0

# ── AWS + Google Sheet (scripts/lambda, scripts/aws) ─────────────────
boto3>=1.43.86
google-api-python-client>=2.200.0
google-auth>=2.57.0
